<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.activities.AboutActivity">

    <include layout="@layout/toolbar" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?attr/windowBackgroundColor"
        android:scrollbarStyle="outsideInset"
        android:scrollbars="vertical">

        <WebView
            android:id="@+id/about_webView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </ScrollView>

    <!--<TextView
            android:id="@+id/about_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/recipe_content_margin_small"
            android:layout_marginEnd="@dimen/recipe_content_margin_small"
            android:layout_marginTop="@dimen/recipe_content_margin_medium"
            android:layout_marginBottom="@dimen/recipe_content_margin_small"
            android:textColor="?attr/textColorMain"
            android:fontFamily="@font/open_sans"
            android:backgroundTint="?attr/tileBackgroundColor" />-->

</LinearLayout>